@model IQueryable<MyShop.Web.Models.ProductViewModel>
<div class="row">
    <div class="col-md-12">
        @foreach (var product in Model)
        {
            @Html.Partial("Product", product)
        }
    </div>
</div>

